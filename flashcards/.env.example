# AWS Credentials for Mono-Repo Deployment
# Copy this file to .env and fill in your actual values

# AWS Credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_ACCOUNT_ID=your_aws_account_id_here
AWS_CLI_PATH=/usr/local/bin/aws

# Deployment
STAGE=dev
AWS_REGION=ap-southeast-2

# Cognito
COGNITO_USER_POOL_ID=your-user-pool-id
COGNITO_CLIENT_ID=your-client-id

# reCAPTCHA v3 (for contact form)
# Get keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

# Contact Form Email (requires verified SES email)
CONTACT_EMAIL=contact@your-domain.com
FROM_EMAIL=noreply@your-domain.com

# Frontend Infrastructure (auto-generated by deploy script)
# These are populated automatically when you run yarn deploy
# S3_BUCKET_DEV=simple-flashcards-frontend-dev
# S3_BUCKET_PROD=simple-flashcards-frontend-prod
# CLOUDFRONT_DISTRIBUTION_ID_DEV=your-distribution-id
# CLOUDFRONT_DISTRIBUTION_ID_PROD=your-distribution-id
# CLOUDFRONT_DOMAIN_DEV=xxx.cloudfront.net
# CLOUDFRONT_DOMAIN_PROD=xxx.cloudfront.net
